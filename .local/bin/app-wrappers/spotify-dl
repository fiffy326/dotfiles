#!/bin/sh

client_id="$(secret-tool lookup spotify-api client_id)"
client_secret="$(secret-tool lookup spotify-api client_secret)"

export SPOTIPY_CLIENT_ID="$client_id"
export SPOTIPY_CLIENT_SECRET="$client_secret"

exec spotify_dl "$@"
