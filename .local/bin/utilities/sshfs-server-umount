#!/bin/sh

if [ -z "$1" ]; then
    server_id=1
else
    server_id="$1"
fi

mount_point="/mnt/server-$server_id"

[ ! -d "$mount_point" ] && exit 0
sudo umount -Rl "$mount_point"
sudo rm -rf "$mount_point"
